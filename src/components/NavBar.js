const NavBar = () => {
    return(
        <nav className="navbar bg-dark text-light mb-5">
            <div className="container-fluid">
                <h1 className="mx-auto">Doris's Weather</h1>
            </div>
        </nav>

    );
}

export default NavBar;
